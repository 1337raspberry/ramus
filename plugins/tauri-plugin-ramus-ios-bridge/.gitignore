/.vs
.DS_Store
.Thumbs.db
*.sublime*
.idea/
debug.log
package-lock.json
.vscode/settings.json
yarn.lock

/.tauri
# Android stub's tauri-api + gradle build outputs are auto-copied/generated
# the first time `cargo tauri android dev` runs. Regenerated per-contributor,
# never tracked — same spirit as the iOS `.tauri` bundle above.
/android/.tauri
/android/build
/target
Cargo.lock
node_modules/

dist-js
dist
